import { styled } from '@/styles';

const Container = styled('div', {
  typo: { size: 12, height: '17px' },
  color: '$darkGrey',
  whiteSpace: 'nowrap',
  flexShrink: 0,
  width: 50,
});

export default Container;
